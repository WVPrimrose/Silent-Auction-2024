// defining variables
// router, and getting express

// direct files to route files (bidder routes, and donation routes)
const router = require('express').Router()

const bidRoutes = require('bidder-routes.js')
